# Database (local)
DB_HOST=localhost
DB_PORT=5432
DB_USER=spotistats
DB_PASSWORD=db_password
DB_NAME=spotistats

# Redis (local)
REDIS_ADDR=localhost:6379

# Kafka (local)
KAFKA_BROKERS=localhost:9092

# App
PORT=8080
